# Core dependencies
faster-whisper>=0.9.0
sounddevice>=0.4.6
pydub>=0.25.0
pynput>=1.7.6
webrtcvad>=2.0.10
pyperclip>=1.8.2
notify2>=0.3.1
numpy>=1.21.0
psutil>=5.9.0

# Voxtral support dependencies
git+https://github.com/huggingface/transformers#egg=transformers
mistral-common[audio]>=1.8.1
accelerate>=1.9.0

# Dia TTS support dependencies
git+https://github.com/nari-labs/dia.git#egg=dia

# ElevenLabs TTS support dependencies
elevenlabs>=1.0.0

# PyTorch (required for Voxtral and Whisper GPU support)
# NOTE: Install PyTorch separately based on your GPU:
#
# For RTX 5090 and newer (CUDA 12.8):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# For RTX 4090 and RTX 40-series (CUDA 12.1):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# For older GPUs (CUDA 11.8):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# CPU-only (no GPU acceleration):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Fallback version (will auto-detect CUDA but may not be optimal):
# Fallback version (will auto-detect CUDA but may not be optimal):
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# MCP (Model Context Protocol) client dependencies
httpx>=0.25.0
sse-starlette>=1.6.0
pydantic>=2.0.0
aiohttp>=3.8.0
websockets>=11.0.0
PyYAML>=6.0.0

# For development/testing (optional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
